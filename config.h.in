#pragma once

#define CMAKE_INSTALL_PREFIX "${CMAKE_INSTALL_PREFIX}"

#define WIIMOTEDEV_VERSION_MAJOR ${WIIMOTEDEV_VERSION_MAJOR}
#define WIIMOTEDEV_VERSION_MINOR ${WIIMOTEDEV_VERSION_MINOR}
#define WIIMOTEDEV_VERSION_PATCH ${WIIMOTEDEV_VERSION_PATCH}
#define WIIMOTEDEV_PROJECT_SOURCE
