<!-- 
-->

<!-- This configuration file specifies the required security policies
     for Wiimotedev daemon to work. -->

<!DOCTYPE busconfig PUBLIC "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>

  <!-- ../system.conf have denied everything, so we just punch some holes -->

  <policy user="root">
    <allow own="org.wiimotedev.daemon"/>
    <allow send_destination="org.wiimotedev.daemon"/>
    <allow send_interface="org.wiimotedev.deviceEvents"/>
    <allow send_interface="org.wiimotedev.service"/>
  </policy>

  <policy group="wiimotedev">
    <allow send_destination="org.wiimotedev.daemon"/>
    <allow send_interface="org.wiimotedev.deviceEvents"/>
    <allow send_interface="org.wiimotedev.service"/>
  </policy>

  <policy context="default">
    <deny send_destination="org.wiimotedev.daemon"/>
  </policy>

</busconfig>